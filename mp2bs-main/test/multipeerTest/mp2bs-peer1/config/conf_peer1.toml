CONFIG_USER_WRR_WEIGHT = [5, 2] # default weight 5, 2

VERBOSE_MODE = false
PACKET_SIZE = 1500 # default size 1500 

HELLO_PACKET = 1
HELLO_ACK_PACKET = 2
DATA_PACKET = 3
GOODBYE_PACKET = 4

NIC_NAMES = ["ethernet", "ethernet"]
NIC_ADDRS = ["172.18.0.3:4343", "172.19.0.3:4344"]
# NIC_NAMES = ["ethernet"]
# NIC_ADDRS = ["172.18.0.3:4343"]

THROUGHPUT_PERIOD = 0.1 # sec
THROUGHPUT_WEIGHT = 0.1 # 0 ~ 1.0
MULTIPATH_THRESHOLD_THROUGHPUT = 10.0 # Mbps
MULTIPATH_THRESHOLD_SEND_COMPLETE_TIME = 2.0 # sec 

CLOSE_TIMEOUT_PERIOD = 200 # ms
